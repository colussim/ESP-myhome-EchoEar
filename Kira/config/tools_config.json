{
  "active_tools": ["get_weather"],
  "definitions": [
    {
      "type": "function",
      "function": {
        "name": "get_weather",
        "description": "Retrieves real-time weather for a city.",
        "parameters": {
          "type": "object",
          "properties": {
            "city": { "type": "string", "description": "The city (ex: Paris, Genève)" }
          },
          "required": ["city"]
        }
      }
    }
  ]
}
